#include<stdio.h>//to check if two integers are equal or not
void main()
{
	int A, B;
		printf("Enter the values for A and B:\n");
		scanf("%d %d", &A, &B);
		if(A == B)
		printf("A and B are equal\n");
		else
		printf("A and B are not equal\n");
}
